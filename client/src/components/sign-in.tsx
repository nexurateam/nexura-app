// Minimal stub replacement for the broken sign-in component.
// The project now uses `WalletAuthCard.tsx` for wallet-only auth UI.
export default function SignInStub() {
  return null;
}

export { SignInStub };